name: tests

on:
  push:
    branches: ["**"]
  pull_request:
  workflow_dispatch:

jobs:
  test:
    runs-on: windows-latest
    env:
      PYTHONUTF8: "1"
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install psutil pystray pillow

      - name: Compile check
        run: python -m py_compile main.py modules/update_helper.py appconfig.py modules/paths.py modules/log_kit.py modules/tray_kit.py

      - name: Import check (module-level config seed must not blow up)
        run: python -c "import main; print('import OK, VERSION =', main.VERSION)"

      - name: Update logic check
        run: |
          python -c "import update_helper as u; assert u._version_is_newer('1.2.0','1.1.0'); assert not u._version_is_newer('1.1.0','1.1.0'); assert u._tag_to_version('v1.2.0')=='1.2.0'; print('update logic OK')"
